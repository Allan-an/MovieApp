<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--    android:layout_marginHorizontal="@dimen/genreItemBorderMargin"-->
<!--
    android:background="@drawable/border"
-->
    <TextView
        android:id="@+id/genreItemTxt"
        android:layout_width="0dp"
        android:layout_height="@dimen/genreItemHeight"
        android:text="@string/genresTitleTxt"
        android:gravity="center"
        android:background="@drawable/button_background_selector"
        android:textSize="@dimen/genreItemTxtSize"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_marginVertical="@dimen/genreItemBorderVerticalMargin"
        android:background="@android:color/holo_purple"
        app:layout_constraintTop_toBottomOf="@+id/genreItemTxt"/>

</androidx.constraintlayout.widget.ConstraintLayout>